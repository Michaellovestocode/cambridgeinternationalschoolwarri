@extends('layouts.app')
@section('title', 'Clinic Incidents')
@section('content')
<div class="mx-auto max-w-6xl space-y-5">
@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('success') }}</div>@endif
<div class="flex flex-col gap-4 rounded-2xl bg-white p-5 shadow sm:flex-row sm:items-center sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-wide text-blue-700">School Clinic</p><h1 class="mt-1 text-2xl font-black">Incident Reports</h1></div><div class="flex gap-2"><a href="{{ route('clinic.dashboard') }}" class="rounded-xl bg-gray-100 px-4 py-3 text-sm font-bold">Dashboard</a><a href="{{ route('clinic.incidents.create') }}" class="rounded-xl bg-amber-400 px-4 py-3 text-sm font-black">New Incident</a></div></div>
<div class="overflow-x-auto rounded-2xl bg-white shadow"><table class="min-w-full text-left text-sm"><thead class="bg-slate-950 text-xs uppercase text-white"><tr><th class="px-4 py-3">Student</th><th class="px-4 py-3">Type</th><th class="px-4 py-3">Location</th><th class="px-4 py-3">Date</th><th class="px-4 py-3">Parent</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($incidents as $incident)<tr><td class="px-4 py-4 font-bold">{{ $incident->student->name }}<span class="block text-xs font-normal text-gray-500">{{ $incident->student->class?->display_name }}</span></td><td class="px-4 py-4">{{ $incident->incident_type }}</td><td class="px-4 py-4">{{ $incident->location ?: '-' }}</td><td class="px-4 py-4">{{ $incident->incident_at->format('j M Y, g:i A') }}</td><td class="px-4 py-4">{{ $incident->parent_contacted ? 'Yes' : 'No' }}</td></tr>@empty<tr><td colspan="5" class="px-4 py-10 text-center text-gray-500">No incidents recorded.</td></tr>@endforelse</tbody></table></div>{{ $incidents->links() }}</div>
@endsection
